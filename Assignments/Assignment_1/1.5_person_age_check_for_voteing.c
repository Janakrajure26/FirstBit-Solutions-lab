#include<stdio.h>
int main(){
	 int age;
	 
	 printf("enter the age");
	 scanf("%d",&age);
	 
	 if (age>=18)
	 printf("person is eligible to vote\n");
	  else
	 printf("person is note eligible to vote\n");
	 
	 return 0; 
}